flatten-packages on windows :
    npm install -g flatten-packages
    flatten-packages

This should eliminate the "deep nested" issues

npm run make : don't run the windows build script, for some reason it won't style the html
